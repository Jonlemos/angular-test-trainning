{
  "name": "db-service",
  "version": "1.0.0",
  "description": "Mock Database for Itaú PJ Dashboard",
  "scripts": {
    "start": "json-server --watch db.json --port 3004",
    "dev": "json-server --watch db.json --port 3004"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
